# --- Docker / VPS runtime ---
# Never commit the deployer keypair or generated chain metadata.
/secrets/
/data/
.env
.env.local
.env.*.local

# Generated chain metadata (per-cluster; provided via the /data volume in prod)
stocklanda_frontend/public/registry.json
stocklanda_frontend/public/flagship.json
